<% form_for :activity, :url => activity_path(@activity), :html => { :method => :put} do |f| %>

  <%= render :partial => 'activity', :locals => {:f => f} %><br/>

  <%= submit_tag 'Update' %> or <%= link_to 'cancel', activities_path %>

<%end%>
