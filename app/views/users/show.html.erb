<h2><%= @user.username %></h2>
<p>Merber since <%= @user.created_at.to_s(:long) %></p>
<%= @user.profile %>

<table id="users">
  <tr>
    <th>Username</th>
    <th>Email</th>
    <th>Enabled?</th>
    <th>Roles</th>
  </tr>
  <%= render :partial => 'user', :collection => @users %>
</table>
